[build]
  base = "frontend"
  command = "npm ci --include=optional --workspaces=false && npm run build --workspaces=false"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
