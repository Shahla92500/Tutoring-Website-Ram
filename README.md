# TutorPro Monorepo

This repository is now split into two top-level folders:

- `frontend/` for the React + Vite application
- `backend/` for the future backend service

## Frontend

Run the frontend from the `frontend/` folder:

```bash
cd frontend
npm run dev
```

## Backend

The backend folder is currently a starter workspace for future API development.
